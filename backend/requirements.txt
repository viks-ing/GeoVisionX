 #OrbitalMind — pinned dependencies
# Install: pip install -r requirements.txt
# GPU (optional): pip install torch==2.2.2+cu118 -f https://download.pytorch.org/whl/torch_stable.html

# ── Core inference ──────────────────────────────────────────────────────────
torch==2.2.2
torchvision==0.17.2
numpy==1.26.4

# ── Geospatial / EO ─────────────────────────────────────────────────────────
rasterio==1.3.10
pyproj==3.6.1
shapely==2.0.4

# ── TerraTorch (TerraMind integration) ──────────────────────────────────────
# terratorch==0.1.0           # uncomment when loading real weights
# huggingface-hub==0.22.2     # for TerraMind-small download

# ── Streamlit demo ──────────────────────────────────────────────────────────
streamlit==1.34.0
Pillow==10.3.0

# ── Metrics + viz ───────────────────────────────────────────────────────────
scikit-learn==1.4.2
matplotlib==3.8.4

# ── Data / datasets ─────────────────────────────────────────────────────────
datasets==2.19.1          # HuggingFace datasets (GEO-Bench, HLS Burn Scars)
# geobench==0.0.4          # uncomment for GEO-Bench SA crop dataset

# ── Utilities ───────────────────────────────────────────────────────────────
tqdm==4.66.4
pyyaml==6.0.1
plotly
streamlit-image-coordinates
pandas
rastervision
